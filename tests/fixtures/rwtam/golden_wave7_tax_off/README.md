# RWTAM golden wave7 tax-off fixture

Wave7 tax-off is refrozen alongside `golden_wave7` for the audit-round-6 BNPL
scenario-only baseline. It preserves the same tax-off extension gate under the
new object stamp.
